[package]
name = "storage"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait.workspace = true
google-cloud-storage.workspace = true
snafu.workspace = true
memo = { path = "../memo" }

[features]
test = []
